libraries <- function(){

      library(XLConnect)
      library(DataCombine)
      library(psych)
      library(scales)
      library(data.table)
      library(plotly)
      library(choroplethr)
      library(choroplethrMaps)
      library(dplyr)
      library(foreach)
    }